import { unifyAddress } from './unify-address'

export const isSameAddress = (addressA?: string | null, addressB?: string | null): boolean =>
  !!addressA && !!addressB && unifyAddress(addressA) === unifyAddress(addressB)
